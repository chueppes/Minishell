#include "../.././includes/minishell.h"

int ft_strsearch(const char *str, char *comp)
{
    int i;
    
    i=0;
    if (!str)
        return(-1);
    while (str[i])
    {
        id(ft_strchr(comp, s[i]))
            return(i);
        i++;
    }
    return(-1);
}